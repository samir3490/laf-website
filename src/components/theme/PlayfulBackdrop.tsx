/** Ambient layer only — all visuals live in playful-theme.css (corner washes, no floating clutter). */
export function PlayfulBackdrop() {
  return <div className="learning-theme-ambient" aria-hidden />;
}
